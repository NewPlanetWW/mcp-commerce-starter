# Copy to .env and fill in your values before running locally.
# Never commit your real .env to git.

# Optional API key auth. Set REQUIRE_AUTH=true to enforce.
API_KEY=dev-secret-key
REQUIRE_AUTH=false
